{
  /* Visit https://firebase.google.com/docs/database/security to learn more about security rules. */
  "rules": {
    ".read": "auth.token.email.endsWith('@sawauniversity.edu.iq')",
    ".write": "auth.token.email.endsWith('@sawauniversity.edu.iq')"
  }
}